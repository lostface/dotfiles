[core]
    autocrlf = input
[color]
    ui = auto
[user]
    name = Zoltan Feher
    email = z.feher@mito.hu
[push]
    default = simple
[pull]
	rebase = preserve
[mergetool]
    prompt = true
    keepBackup = false
    keepTemporaries = false
[merge]
    tool = diffmerge
[mergetool "diffmerge"]
    cmd = "~/dev/tools/diff/dm_mergetool.sh" "$MERGED" "$LOCAL" "$BASE" "$REMOTE"
[diff]
    tool = diffmerge
[difftool "diffmerge"]
    cmd = "~/dev/tools/diff/kd_difftool.sh" "$LOCAL" "$REMOTE"
[alias]
    ch = checkout
    br = branch
